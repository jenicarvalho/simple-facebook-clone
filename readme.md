# Simple Facebook Clone With React

It's a pretty simple facebook page built using React + Babel + Webpack from scratch.

## Installation

Use the package manager [yarn](https://yarnpkg.com) to install.

```bash
yarn
```

## Usage

```bash
yarn dev
```
